{
	"name": "qlogicae-cor1",
	"version": "1.2.9",
	"author": "Vaughn Cedric L. Araneta",
	"main": "./qlogicae/cor1/index.js",
	"module": "index.ts",
	"devDependencies": {
		"@eslint/js": "^10.0.1",
		"@types/bun": "latest",
		"@types/node": "^26.0.0",
		"@vitest/coverage-v8": "4.1.9",
		"esbuild": "^0.28.1",
		"eslint": "^10.5.0",
		"javascript-obfuscator": "^5.4.3",
		"prettier": "^3.8.4",
		"rimraf": "^6.1.3",
		"tsup": "^8.5.1",
		"typescript": "^6.0.3",
		"typescript-eslint": "^8.61.1",
		"vitest": "^4.1.9"
	},
	"bin": {
		"qlogicae-cor1": "./qlogicae/cor1/version.js"
	},
	"description": "The core library for the QLogicae framework",
	"license": "MIT",
	"private": false,
	"scripts": {
		"login": "npm login",
		"clean:all": "bun run clean:build && bun run clean:reports",
		"clean:build": "bunx rimraf ./.outputs/build",
		"clean:reports": "bunx rimraf ./.outputs/reports",
		"format": "bunx prettier --config './.qlogicae/public/configurations/.prettierrc' --write .",
		"format:check": "bunx prettier --config './.qlogicae/public/configurations/.prettierrc' --check .",
		"lint": "bunx eslint --config './.qlogicae/public/configurations/eslint.config.js' .",
		"transpile": "bunx tsc --project './.qlogicae/public/configurations/tsconfig.json'",
		"bundle": "bun './.qlogicae/public/configurations/esbuild.js'",
		"build:pre": "bun run clean:build && bun run format && bun run lint",
		"build": "bun run build:pre && bun run transpile && bun run bundle && bun run build:post",
		"build:post": "cp package.json ./.outputs/build && cp ../README.md ./.outputs/build && cp -r ../qlogicae ./.outputs/build",
		"test:all": "bun run test:unit && bun run test:benchmark",
		"test:unit": "bunx vitest run --watch=false --config './.qlogicae/public/configurations/vitest.unit.config.ts' --reporter=verbose --coverage",
		"test:benchmark": "bunx vitest bench --watch=false --config './.qlogicae/public/configurations/vitest.bench.config.ts' --coverage",
		"deploy:tag": "git tag \"typescript-v$(node -p \"require('./package.json').version\")\"",
		"deploy:push": "git push origin && git push origin \"typescript-v$(node -p \"require('./package.json').version\")\"",
		"deploy:check": "bun run build && npm pack ./.outputs/build --dry-run",
		"deploy:publish": "bun publish --cwd ./.outputs/build && bun run deploy:push",
		"deploy:commit": "git add . && git commit -m 'deployment preparation' || true",
		"workflow:develop": "bun run build",
		"workflow:test": "bun run build && bun run test",
		"workflow:test:unit": "bun run build && bun run test:unit",
		"workflow:test:benchmark": "bun run build && bun run test:benchmark",
		"workflow:deploy:pre": "bun run build",
		"workflow:deploy": "bun run workflow:deploy:pre && bun run deploy:commit && bun run deploy:tag && bun run workflow:deploy:post",
		"workflow:deploy:post": "(bun run deploy:publish) || (bun run login && bun run deploy:publish)"
	},
	"type": "module",
	"types": "./qlogicae/cor1/index.d.ts",
	"keywords": [
		"qlogicae",
		"framework",
		"library",
		"package",
		"typescript",
		"javascript"
	]
}
